/**
 * Created by DENIS on 11/04/2017.
 */

// since we used a table, we can't use labels to wrap the input and corresponding text

// document.addEventListener("DOMContentLoaded", function () {
//   var rows = document.querySelectorAll(".poll-table tr");
//
//   rows.forEach(function (row) {
//     row.addEventListener("click", function () {
//       var childInput = this.querySelector("input");
//
//       // if(childInput.type == 'radio')
//         //childInput.setAttribute("checked", "");
//     });
//   });
// });